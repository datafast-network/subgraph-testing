{
  "name": "mono-subgraph-test",
  "version": "1.0.0",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "pnpm run -r build",
    "cp": "pnpm run -r cp",
    "build-test": "pnpm build && pnpm cp"
  },
  "packageManager": "pnpm@8.8.0"
}
