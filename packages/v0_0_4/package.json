{
  "name": "v4-subgraph",
  "version": "1.0.0",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "graph codegen --output-dir src/types/ && graph build",
    "cp": "mv ./build/Factory/Factory.wasm ../../wasm/test_0_0_4.wasm"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
